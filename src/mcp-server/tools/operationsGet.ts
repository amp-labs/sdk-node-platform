/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { operationsGet } from "../../funcs/operationsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOperationRequest$inboundSchema,
};

export const tool$operationsGet: ToolDefinition<typeof args> = {
  name: "operations-get",
  description: `Get an operation`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await operationsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
