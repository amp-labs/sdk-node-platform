/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauthUpdate } from "../../funcs/oauthUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OauthUpdateRequest$inboundSchema,
};

export const tool$oauthUpdate: ToolDefinition<typeof args> = {
  name: "oauth-update",
  description: `Get URL for updating OAuth connection

Generate a URL for the browser to render to kick off OAuth flow that updates an existing connection.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauthUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
