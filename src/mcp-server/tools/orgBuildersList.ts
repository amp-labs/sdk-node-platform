/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { orgBuildersList } from "../../funcs/orgBuildersList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListOrgBuildersRequest$inboundSchema,
};

export const tool$orgBuildersList: ToolDefinition<typeof args> = {
  name: "org-builders-list",
  description: `List builders for an organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await orgBuildersList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
