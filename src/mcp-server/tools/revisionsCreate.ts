/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { revisionsCreate } from "../../funcs/revisionsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateRevisionRequest$inboundSchema,
};

export const tool$revisionsCreate: ToolDefinition<typeof args> = {
  name: "revisions-create",
  description: `Create a new revision.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await revisionsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
