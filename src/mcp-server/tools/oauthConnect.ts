/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauthConnect } from "../../funcs/oauthConnect.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OauthConnectRequestBody$inboundSchema,
};

export const tool$oauthConnect: ToolDefinition<typeof args> = {
  name: "oauth-connect",
  description: `Get URL for OAuth flow

Generate a URL for the browser to render to kick off OAuth flow.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauthConnect(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
