/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { installationsUpdate } from "../../funcs/installationsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateInstallationRequest$inboundSchema,
};

export const tool$installationsUpdate: ToolDefinition<typeof args> = {
  name: "installations-update",
  description: `Update an installation`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await installationsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
