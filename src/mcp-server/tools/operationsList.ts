/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { operationsList } from "../../funcs/operationsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListOperationsRequest$inboundSchema,
};

export const tool$operationsList: ToolDefinition<typeof args> = {
  name: "operations-list",
  description: `List operations`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await operationsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
