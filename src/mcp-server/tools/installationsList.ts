/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { installationsList } from "../../funcs/installationsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListInstallationsRequest$inboundSchema,
};

export const tool$installationsList: ToolDefinition<typeof args> = {
  name: "installations-list",
  description: `List installations`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await installationsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
