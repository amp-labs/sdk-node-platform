/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { installationUnpauseReads } from "../../funcs/installationUnpauseReads.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UnpauseReadsRequest$inboundSchema,
};

export const tool$installationUnpauseReads: ToolDefinition<typeof args> = {
  name: "installation-unpause-reads",
  description: `Unpause reads for an installation

Unpauses all reads for an installation. If no reads were paused, this will be a no-op. Accepts specific objects to unpause reads for in the request body.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await installationUnpauseReads(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
