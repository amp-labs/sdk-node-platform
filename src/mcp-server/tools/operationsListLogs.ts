/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { operationsListLogs } from "../../funcs/operationsListLogs.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListOperationLogsRequest$inboundSchema,
};

export const tool$operationsListLogs: ToolDefinition<typeof args> = {
  name: "operations-list-logs",
  description: `List logs for an operation`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await operationsListLogs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
