/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { uploadUrlsGenerate } from "../../funcs/uploadUrlsGenerate.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$uploadUrlsGenerate: ToolDefinition = {
  name: "upload-urls-generate",
  description: `Generate a signed URL to upload a zip file to.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await uploadUrlsGenerate(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
