/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { consumersCreate } from "../../funcs/consumersCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateConsumerRequest$inboundSchema,
};

export const tool$consumersCreate: ToolDefinition<typeof args> = {
  name: "consumers-create",
  description: `Create a new consumer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await consumersCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
