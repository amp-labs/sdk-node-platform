/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { installationPauseReads } from "../../funcs/installationPauseReads.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PauseReadsRequest$inboundSchema,
};

export const tool$installationPauseReads: ToolDefinition<typeof args> = {
  name: "installation-pause-reads",
  description: `Pause reads for an installation

Pauses all reads for an installation. If reads are already paused, this will be a no-op. Accepts specific objects to pause reads for in the request body.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await installationPauseReads(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
