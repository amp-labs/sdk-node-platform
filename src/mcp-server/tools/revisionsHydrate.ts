/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { revisionsHydrate } from "../../funcs/revisionsHydrate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetHydratedRevisionRequest$inboundSchema,
};

export const tool$revisionsHydrate: ToolDefinition<typeof args> = {
  name: "revisions-hydrate",
  description: `Hydrate a revision

Hydrate a revision with information from the consumer's SaaS instance.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await revisionsHydrate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
